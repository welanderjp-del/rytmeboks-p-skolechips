import { getState } from '../state.js';
import { renderGrid } from '../grid/grid.js';

// This file can be used for mode-specific logic if it grows
export function initNormalMode() {
    // Normal mode specific init if needed
}
